@AGENTS.md

## Agent skills

### Issue tracker

GitHub — los issues viven en GitHub Issues (usa la CLI `gh`). See `docs/agents/issue-tracker.md`.

### Triage labels

Etiquetas por defecto (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`). See `docs/agents/triage-labels.md`.

### Domain docs

single-context. See `docs/agents/domain.md`.
